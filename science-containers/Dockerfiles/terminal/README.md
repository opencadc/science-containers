# Terminal Desktop Application

This directory contains a Dockerfile for building a containerized version of a terminal desktop application. This setup allows users to run the terminal application in an isolated environment, ensuring consistency across different systems.